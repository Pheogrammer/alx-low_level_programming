#include "main.h"
#include <ctype.h>

/**
 * mul - checks for digit character.
 *
 * @c: character to check.
 *
 * @b: character to check.
 *
 * Return: Always 0.
 */
int mul(int c, int b)
{
	int x;

	x = mul(c, b);
	return (x);
}
